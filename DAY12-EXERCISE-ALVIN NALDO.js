//-------------------------UNIT TESTING-----------------------------

let multiple = (numA, numB) => {
  return numA * numB;
};

let add = (numA, numB) => {
  return numA + numB;
};

module.exports = { add, multiple };
